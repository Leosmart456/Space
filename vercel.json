{
  "version": 2,
  "buildCommand": "npm run build && npx esbuild api/index.ts --bundle --platform=node --packages=external --format=esm --outfile=dist/server.js",
  "builds": [
    {
      "src": "dist/server.js",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb",
        "includeFiles": ["dist/public/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/server.js"
    }
  ]
}
